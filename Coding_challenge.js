const arrayIndex = {
    indOne: 1,
    indTwo: 10,
    indThree: 100,
    indFour: 1000,
    indFive: 10000,
    indSix: 1000000,

};

const keyView = Object.keys(arrayIndex);

console.log(keyView);

const valueView = Object.values(arrayIndex);

console.log(valueView);

const objectView = Object.entries(arrayIndex);

console.log(objectView);